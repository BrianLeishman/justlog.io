{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "go run ../go/cmd/compile-ts/ && hugo --minify",
    "deploy": "npm run build && aws s3 sync public/ s3://justlog.io/ --delete && aws cloudfront create-invalidation --distribution-id E1LT80V2EBQZX0 --paths '/*'",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "bootstrap": "^5.3.8"
  },
  "devDependencies": {
    "@eslint/js": "^9.23.0",
    "@stylistic/eslint-plugin": "^4.4.0",
    "eslint": "^9.23.0",
    "globals": "^16.0.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.30.0"
  }
}
