{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "go run ../go/cmd/compile-ts/ && hugo --minify",
    "deploy:api": "cd .. && AWS_PROFILE=justlog bash scripts/deploy-api.sh",
    "deploy:mcp": "cd .. && AWS_PROFILE=justlog bash scripts/deploy-mcp.sh",
    "deploy:site": "cd .. && AWS_PROFILE=justlog bash scripts/deploy-site.sh",
    "deploy:all": "npm run deploy:api && npm run deploy:mcp && npm run deploy:site",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "bootstrap": "^5.3.8"
  },
  "devDependencies": {
    "@eslint/js": "^9.23.0",
    "@stylistic/eslint-plugin": "^4.4.0",
    "eslint": "^9.23.0",
    "globals": "^16.0.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.30.0"
  }
}
